syslog:
  server: example.com
  port: 514
  enable: true

user:
  - name: Administrator
    password: foobar012
    role: admin
    enable: true
  - name: Guest
    password: foob4r
    role: admin
    enable: false

ldap:
  server: example.com
  port: 636
  enable: true
  role: admin
